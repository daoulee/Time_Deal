# 프론트가 Render 백엔드에 연결할 때 필요한 공개 환경변수 형식을 보여주는 예시입니다.
# VITE_API_BASE_URL만 복사해 실제 `.env`에 입력하고 이 예시 파일은 Git에 추적합니다.
# Supabase service role key나 서버 비밀값을 VITE 변수로 넣으면 안 됩니다.
VITE_API_BASE_URL=https://YOUR-RENDER-SERVICE.onrender.com
